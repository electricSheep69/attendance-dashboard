cd /Users/pankajs/attendance-dashboard

# Add the remote (replace YOUR_USERNAME with your GitHub username)
git remote add origin https://github.com/electricSheep69/attendance-dashboard.git

# Create a .gitignore file to exclude system files
cat > .gitignore << 'EOF'
.DS_Store
.claude/
node_modules/
.env
EOF

# Stage and commit all files
git add .
git commit -m "Initial commit"

# Push to GitHub
git branch -M main
git push -u origin main